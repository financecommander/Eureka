{
  "name": "eureka-settlement-verifier",
  "version": "1.0.0",
  "description": "Settlement verification chain with smart contracts",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.20.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^6.10.0",
    "merkletreejs": "^0.3.11",
    "keccak256": "^1.0.6"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-etherscan": "^3.1.7",
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  }
}
